---
import BaseLayout from '../layouts/BaseLayout.astro';
import GlitchText from '../components/GlitchText.astro';
import Card from '../components/Card.astro';
import DifficultyBadge from '../components/DifficultyBadge.astro';
import ScrollProgressTrail from '../components/ScrollProgressTrail.astro';
---

<BaseLayout title="Build Your Tech Fence" activePage="home">
  <ScrollProgressTrail />

  <!-- ====== HERO SECTION ====== -->
  <section class="cine-section hero" data-section-id="hero" data-section-label="Home">
    <div class="cine-heading">
      <div class="hero-label">// BY AN IGNORANT, FOR THE IGNORANTS. INSPIRED BY OUR SAVIOR'S TECH JOURNEY.</div>
      <GlitchText text="Build Your Tech Fence" tag="h1" class="hero-title" />
    </div>
    <div class="cine-content">
      <p class="hero-subtitle">
        Build your fence. Own your data. Control your digital life.
      </p>
      <div class="hero-quote-block">
        <img
          src="/images/pewdiepie-savior.webp"
          alt="PewDiePie"
          class="hero-sticker"
          width="200"
          height="200"
          loading="eager"
        />
        <blockquote class="hero-quote">
          <p>
            "I think you owe it to yourself to take some time today and start
            building your tech fence. These tech companies don't care about you.
            So, you're going to have to start caring about yourself. The cheat
            sheet code is basically build some friction. It's that simple. Let
            your brain catch up and make the choice yourself. And also filter out
            the noise. That's your defense and that's your cure. Now you
            understand the disease. You are free."
          </p>
          <cite>— PewDiePie, "I Fixed YouTube!"</cite>
        </blockquote>
      </div>
      <div class="hero-actions">
        <a href="#pillars" class="btn btn-primary hero-scroll-btn" data-target="pillars">Start Here — 5 Minutes</a>
        <a href="#extras" class="btn btn-secondary hero-scroll-btn" data-target="extras">I Just Want the TL;DR</a>
      </div>
    </div>
  </section>

  <div class="section-spacer"></div>

  <!-- ====== TWO PILLARS ====== -->
  <section id="pillars" class="cine-section" data-section-id="pillars" data-section-label="Two Pillars">
    <div class="container">
      <div class="cine-heading">
        <h2 class="section-heading glitch-hover" data-text="The Two Pillars">The Two Pillars</h2>
      </div>
      <div class="cine-content">
        <blockquote class="section-quote">
          <p>
            "If you go around your life not making your own choices, then who the
            heck are you? You are your choices and your actions. That's what
            shapes who you are."
          </p>
          <cite>— PewDiePie, "I Fixed YouTube!"</cite>
        </blockquote>

        <div class="pillars-layout">
          <img
            src="/images/pewdiepie-matrix.webp"
            alt="PewDiePie offering the red and blue pill"
            class="pillars-bg-sticker"
            width="500"
            height="375"
            loading="lazy"
          />
          <div class="grid grid-2 pillars-grid">
            <div class="pillar-card">
              <div class="pillar-number">01</div>
              <h3 class="pillar-title">You Are Not The Product</h3>
              <div class="flowing-line"></div>
              <p>
                Every "free" service has a cost — your data, your attention, your autonomy.
              </p>
              <p class="pillar-quote">
                "If it's so great then why do I feel like I'm the product? I hate my phone. Notifications just feel like they're there to suck me in..."
                <span class="quote-attr">— PewDiePie, "I'm DONE with Google"</span>
              </p>
            </div>

            <div class="pillar-card">
              <div class="pillar-number">02</div>
              <h3 class="pillar-title">Curing Algo Brain</h3>
              <div class="flowing-line"></div>
              <p>
                The cure is <strong>friction</strong> and <strong>filtering</strong>. Let your brain catch up. Break the loop.
              </p>
              <p class="pillar-quote">
                "I used to catch myself all the time being on my phone without intending to and I get so pissed off and I'm like, What the fuck?"
                <span class="quote-attr">— PewDiePie, "I Fixed YouTube!"</span>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="section-spacer"></div>

  <!-- ====== THREE LAYERS ====== -->
  <section class="cine-section" data-section-id="layers" data-section-label="Three Layers">
    <div class="container">
      <div class="cine-heading">
        <h2 class="section-heading glitch-hover" data-text="The Three Layers">The Three Layers</h2>
      </div>
      <div class="cine-content">
        <blockquote class="section-quote">
          <p>
            "I think you owe it to yourself to take some time today and start
            building your tech fence. That's what I'm calling this — tech fencing."
          </p>
          <cite>— PewDiePie, "I Fixed YouTube!"</cite>
        </blockquote>

        <div class="layers-layout">
          <img
            src="/images/pewdiepie-revelation.webp"
            alt="PewDiePie revelation moment"
            class="layers-bg-sticker"
            width="500"
            height="400"
            loading="lazy"
          />
          <div class="grid grid-3 layers-grid">
            <Card title="Layer 1: OS Control" href="/bible/layer-1-os" accent class="layer-card">
              <img src="/images/linux-os.webp" alt="" class="layer-card-bg layer-bg-linux" loading="lazy" />
              <p>
                Replace Windows with Linux. Choose an OS that doesn't spy on you,
                ship ads, or decide what you can install.
              </p>
              <p class="card-source">Based on "I installed Linux (so should you)"</p>
            </Card>

            <Card title="Layer 2: Service Control" href="/bible/layer-2-services" accent class="layer-card">
              <img src="/images/de-google.webp" alt="" class="layer-card-bg layer-bg-google" loading="lazy" />
              <p>
                De-Google your life. Self-host alternatives for passwords, cloud
                storage, email, and more. Own your data instead of renting it.
              </p>
              <p class="card-source">Based on "I'm DONE with Google"</p>
            </Card>

            <Card title="Layer 3: Behavior Control" href="/bible/layer-3-behavior" accent class="layer-card">
              <img src="/images/brain-control.webp" alt="" class="layer-card-bg layer-bg-brain" loading="lazy" />
              <p>
                Fight algorithmic feeds. Add friction to mindless scrolling. Reclaim
                your attention from apps designed to steal it.
              </p>
              <p class="card-source">Based on "I Fixed YouTube!"</p>
            </Card>
          </div>
        </div>

        <div class="section-cta">
          <a href="/bible" class="btn btn-secondary">Explore The Bible →</a>
        </div>
      </div>
    </div>
  </section>

  <div class="section-spacer"></div>

  <!-- ====== GETTING STARTED (slim) ====== -->
  <section class="cine-section" data-section-id="guides" data-section-label="Getting Started">
    <div class="container">
      <div class="cine-heading">
        <h2 class="section-heading glitch-hover" data-text="Getting Started">Getting Started</h2>
      </div>
      <div class="cine-content">
        <p class="section-intro">
          You don't need to become a cybersecurity expert overnight. Here are
          some actionable guides that will inform your decisions.
        </p>

        <div class="guides-layout">
          <img
            src="/images/pewdiepie-tinfoil.webp"
            alt="PewDiePie with tinfoil hat"
            class="guides-bg-sticker"
            width="500"
            height="400"
            loading="lazy"
          />
          <div class="grid grid-3 guides-grid">
          <a href="/guides/browsers" class="guide-preview-card">
            <img src="/images/firefox-logo.webp" alt="" class="guide-card-bg guide-bg-firefox" loading="lazy" />
            <DifficultyBadge level={1} />
            <h3>1 — Switch Your Browser</h3>
            <p>Install Firefox or Brave. Change your search engine. Install uBlock Origin. Takes 5 minutes.</p>
          </a>

          <a href="/guides/linux" class="guide-preview-card">
            <img src="/images/archlinux-logo.webp" alt="" class="guide-card-bg guide-bg-arch" loading="lazy" />
            <DifficultyBadge level={2} />
            <h3>2 — Install Linux</h3>
            <p>Replace Windows with an OS that respects you. A weekend project that changes everything.</p>
          </a>

          <a href="/guides/de-google" class="guide-preview-card">
            <img src="/images/opensource-logo.webp" alt="" class="guide-card-bg guide-bg-opensource" loading="lazy" />
            <DifficultyBadge level={3} />
            <h3>3 — De-Google Your Life</h3>
            <p>Replace Google services with open-source alternatives you can trust. An afternoon of work.</p>
          </a>
          </div>
        </div>

        <div class="section-cta">
          <a href="/guides" class="btn btn-secondary">See All 9 Guides →</a>
        </div>
      </div>
    </div>
  </section>

  <div class="section-spacer"></div>

  <!-- ====== HONEST DISCLAIMER ====== -->
  <section class="cine-section" data-section-id="honesty" data-section-label="Honesty">
    <div class="container">
      <div class="cine-heading">
        <h3 class="disclaimer-heading glitch-hover" data-text="A Word of Honesty">A Word of Honesty</h3>
      </div>
      <div class="cine-content">
        <div class="honesty-layout">
          <img
            src="/images/pewdiepie-honesty.webp"
            alt="PewDiePie being honest"
            class="honesty-bg-sticker"
            width="500"
            height="400"
            loading="lazy"
          />
        <div class="disclaimer-box">
          <p>
            This isn't easy, and it's not for everyone. PewDiePie himself still
            uses Google Maps because "it's too good." He still uses YouTube — "I
            literally can't escape." His Vaultwarden setup took 2 days. He was 30
            minutes late somewhere because the Google Maps alternative was terrible.
          </p>
          <p>
            I tell you the downsides because I respect you. Every guide on this
            site includes an <strong>"Honest Downsides"</strong> section. If
            something sucks, I'll say so.
          </p>
          <p>
            Also — I'm learning too. This site was made by a fan who watched the
            videos and decided to actually do it. I'm not an expert. I'm just
            someone who built the fence.
          </p>
          <p>
            One more thing — this site was vibecoded. The code was AI-generated,
            but every piece of information, every recommendation, and every design
            choice was intentionally curated by me, a fellow ignorant.
          </p>
        </div>
        </div>
      </div>
    </div>
  </section>

  <div class="section-spacer"></div>

  <!-- ====== EXTRAS ====== -->
  <section id="extras" class="cine-section" data-section-id="extras" data-section-label="Extras">
    <div class="container">
      <div class="cine-heading">
        <h2 class="section-heading glitch-hover" data-text="Extras">Extras</h2>
      </div>
      <div class="cine-content">
        <p class="section-intro">
          More resources, references, and rabbit holes.
        </p>

        <div class="extras-grid">
          <a href="/tldr" class="extra-card" data-emoji="⚡">
            <h3>TL;DR</h3>
            <p>Read only this page — it contains everything you need to know.</p>
          </a>

          <a href="/arsenal" class="extra-card" data-emoji="🛡️">
            <h3>The Arsenal</h3>
            <p>Every piece of hardware and software PewDiePie actually uses.</p>
          </a>

          <a href="/damned" class="extra-card" data-emoji="💀">
            <h3>The Damned</h3>
            <p>Services we don't recommend — and the honest reasons why.</p>
          </a>

          <a href="/glossary" class="extra-card" data-emoji="📖">
            <h3>Glossary</h3>
            <p>Every tech term on this site, explained without jargon.</p>
          </a>

          <a href="/about" class="extra-card" data-emoji="👋">
            <h3>About</h3>
            <p>Who built this site, why, and what we believe.</p>
          </a>

          <div class="extras-brofist-cell">
            <img
              src="/images/pewdiepie-brofist.webp"
              alt="PewDiePie brofist"
              class="extras-brofist"
              width="300"
              height="400"
              loading="lazy"
            />
          </div>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  import { ScrollToPlugin } from 'gsap/ScrollToPlugin';

  gsap.registerPlugin(ScrollTrigger, ScrollToPlugin);

  // Expose section triggers globally for progress trail
  (window as any).__sectionTriggers = {};

  function initCinematicScroll() {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (prefersReducedMotion) {
      document.querySelectorAll<HTMLElement>('.cine-content').forEach(el => {
        el.style.opacity = '1';
      });
      return;
    }

    const sections = gsap.utils.toArray<HTMLElement>('.cine-section');
    const sectionTriggers: ScrollTrigger[] = [];

    // ---- Global timing config (tweak these to control all animations) ----
    // These are relative units — actual speed depends on scroll distance (end value).
    // Total timeline = enter + reveal + hold + exit. Each section's scroll range
    // is divided proportionally across these phases.
    const TIMING = {
      enterDuration: 2.0,    // how long heading takes to slide in
      revealDuration: 1.4,    // how long content takes to appear
      holdDuration: 1.35,     // reading time before exit starts
      exitDuration: 1.4,      // how long exit animation takes
      slideDistance: 200,      // px distance for horizontal slides
      headingLift: -80,       // px heading moves up during exit
      scrollRange: '+=100%',  // scroll distance per section (viewport %)
    };

    // Measure navbar height so hero pins immediately (no free-scroll gap)
    const navbar = document.querySelector('.navbar');
    const navHeight = navbar ? navbar.getBoundingClientRect().height : 0;

    sections.forEach((section, i) => {
      const heading = section.querySelector('.cine-heading') as HTMLElement;
      const content = section.querySelector('.cine-content') as HTMLElement;
      if (!heading || !content) return;

      const isHero = section.dataset.sectionId === 'hero';
      const isLast = i === sections.length - 1;
      const sectionId = section.dataset.sectionId || `section-${i}`;

      // Hero: pin as soon as page starts scrolling (offset by navbar height)
      // Others: pin when section top reaches viewport top
      const pinStart = isHero ? `top top+=${navHeight}` : 'top top';

      const tl = gsap.timeline({
        scrollTrigger: {
          trigger: section,
          start: pinStart,
          end: TIMING.scrollRange,
          pin: true,
          pinSpacing: true,
          scrub: 2.5,
          invalidateOnRefresh: true,
          id: sectionId,
        },
      });

      // Store the trigger
      if (tl.scrollTrigger) {
        sectionTriggers.push(tl.scrollTrigger);
        (window as any).__sectionTriggers[sectionId] = tl.scrollTrigger;
      }

      if (isHero) {
        // Hero: visible from start, fades out vertically
        gsap.set(content, { opacity: 1, y: 0 });

        tl.addLabel('visible')
          .to(heading, { y: -60, opacity: 0, duration: TIMING.exitDuration }, 0.65)
          .to(content, { y: -40, opacity: 0, duration: TIMING.exitDuration }, 0.65);
      } else {
        // Non-hero sections: horizontal slideshow transitions
        gsap.set(heading, { x: TIMING.slideDistance, opacity: 0 });
        gsap.set(content, { x: TIMING.slideDistance * 0.75, opacity: 0 });

        // Phase 1: Heading slides in from right
        tl.addLabel('enter')
          .to(heading, { x: 0, opacity: 1, duration: TIMING.enterDuration, ease: 'power2.out' })

        // Phase 2: Content slides in (heading stays in place)
          .addLabel('reveal')
          .to(content, { x: 0, opacity: 1, duration: TIMING.revealDuration, ease: 'power2.out' })

        // Phase 3: Hold for reading (heading + content both fully visible)
          .addLabel('hold')
          .to({}, { duration: TIMING.holdDuration });

        // Phase 4: Exit — heading lifts up, then everything slides left
        tl.addLabel('exit')
          .to(heading, { y: TIMING.headingLift, opacity: 0, duration: TIMING.exitDuration, ease: 'power1.in' })
          .to(content, { x: -TIMING.slideDistance, opacity: 0, duration: TIMING.exitDuration, ease: 'power1.in' }, '<0.05');
      }
    });

    ScrollTrigger.refresh();

    // ---- Cinematic Mode (auto-scroll on scroll input) ----
    let cinematicMode = localStorage.getItem('cinematicMode') !== 'false';
    let isAutoScrolling = false;
    let currentSectionIndex = 0;

    function getCinematicMode() {
      return localStorage.getItem('cinematicMode') !== 'false';
    }

    function findCurrentSectionIndex() {
      const scrollY = window.scrollY;
      const maxScroll = document.documentElement.scrollHeight - window.innerHeight;

      // If we're near the bottom (footer area), return totalSections (footer index)
      if (scrollY >= maxScroll - 50) {
        return totalSections;
      }

      // If we're past the last section's end, we're in the footer zone
      const lastST = sectionTriggers[sectionTriggers.length - 1];
      if (lastST && scrollY > lastST.end) {
        return totalSections;
      }

      let idx = 0;
      for (let i = 0; i < sectionTriggers.length; i++) {
        if (scrollY >= sectionTriggers[i].start - 10) {
          idx = i;
        }
      }
      return idx;
    }

    // Scroll target: 50% into section range (where content is fully revealed)
    // For hero: go to start. For footer: go to document bottom.
    const CONTENT_VISIBLE_RATIO = 0.5;
    const totalSections = sectionTriggers.length; // includes footer pseudo-target

    function getScrollTarget(index: number): number {
      if (index < 0) return 0;
      // Footer: scroll to document bottom
      if (index >= totalSections) {
        return document.documentElement.scrollHeight - window.innerHeight;
      }
      const st = sectionTriggers[index];
      const sId = sections[index]?.dataset.sectionId;
      if (sId === 'hero') return st.start;
      return st.start + (st.end - st.start) * CONTENT_VISIBLE_RATIO;
    }

    function handleWheel(e: WheelEvent) {
      cinematicMode = getCinematicMode();
      if (!cinematicMode || isAutoScrolling) {
        if (isAutoScrolling) e.preventDefault();
        return;
      }
      e.preventDefault();
      isAutoScrolling = true;

      currentSectionIndex = findCurrentSectionIndex();
      const direction = e.deltaY > 0 ? 1 : -1;
      // Allow scrolling one past the last section (to reach footer)
      const maxIndex = totalSections; // totalSections = footer
      const nextIndex = Math.max(0, Math.min(maxIndex, currentSectionIndex + direction));

      if (nextIndex === currentSectionIndex) {
        isAutoScrolling = false;
        return;
      }

      currentSectionIndex = nextIndex;

      gsap.to(window, {
        scrollTo: { y: getScrollTarget(nextIndex) },
        duration: 0.0,
        ease: 'power2.inOut',
        onComplete: () => {
          setTimeout(() => { isAutoScrolling = false; }, 100);
        },
      });
    }

    // Touch handling for cinematic mode
    let touchStartY = 0;

    function handleTouchStart(e: TouchEvent) {
      touchStartY = e.touches[0].clientY;
    }

    function handleTouchEnd(e: TouchEvent) {
      cinematicMode = getCinematicMode();
      if (!cinematicMode || isAutoScrolling) return;

      const touchEndY = e.changedTouches[0].clientY;
      const diff = touchStartY - touchEndY;

      if (Math.abs(diff) < 30) return; // too small, ignore

      isAutoScrolling = true;
      currentSectionIndex = findCurrentSectionIndex();
      const direction = diff > 0 ? 1 : -1;
      const maxIndex = totalSections;
      const nextIndex = Math.max(0, Math.min(maxIndex, currentSectionIndex + direction));

      if (nextIndex === currentSectionIndex) {
        isAutoScrolling = false;
        return;
      }

      currentSectionIndex = nextIndex;

      gsap.to(window, {
        scrollTo: { y: getScrollTarget(nextIndex) },
        duration: 0.0,
        ease: 'power2.inOut',
        onComplete: () => {
          setTimeout(() => { isAutoScrolling = false; }, 100);
        },
      });
    }

    // Attach cinematic listeners
    window.addEventListener('wheel', handleWheel, { passive: false });
    window.addEventListener('touchstart', handleTouchStart, { passive: true });
    window.addEventListener('touchend', handleTouchEnd, { passive: true });

    // Listen for toggle from navbar
    window.addEventListener('cinematicModeChange', ((e: CustomEvent) => {
      cinematicMode = e.detail.enabled;
    }) as EventListener);
  }

  // Init
  initCinematicScroll();

  // Hero scroll buttons — scroll to target section
  document.querySelectorAll<HTMLAnchorElement>('.hero-scroll-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      const targetId = btn.dataset.target;
      if (!targetId) return;

      const cinematicOn = localStorage.getItem('cinematicMode') !== 'false';
      const targetTrigger = (window as any).__sectionTriggers?.[targetId] as ScrollTrigger | undefined;

      if (cinematicOn && targetTrigger) {
        // In cinematic mode, scroll to the trigger's content-visible position
        const scrollTarget = targetTrigger.start + (targetTrigger.end - targetTrigger.start) * 0.5;
        gsap.to(window, { scrollTo: { y: scrollTarget }, duration: 0.0, ease: 'power2.inOut' });
      } else {
        // Fallback: native smooth scroll to the element
        const el = document.getElementById(targetId);
        el?.scrollIntoView({ behavior: 'smooth' });
      }
    });
  });

  // When browser blocker is dismissed, main-app goes from display:none to visible.
  // GSAP measured everything as zero while hidden — rebuild all ScrollTriggers.
  window.addEventListener('blocker-dismissed', () => {
    ScrollTrigger.getAll().forEach(st => st.kill());
    (window as any).__sectionTriggers = {};
    initCinematicScroll();
  });

  // Astro page transition support
  document.addEventListener('astro:after-swap', () => {
    ScrollTrigger.getAll().forEach(st => st.kill());
    (window as any).__sectionTriggers = {};
    if (document.querySelector('.cine-section')) {
      initCinematicScroll();
    }
  });
</script>

<style>
  /* ---- Layout overrides (homepage only) ---- */
  :global(#main-content) {
    overflow: visible;
  }

  /* ---- Section Spacers ---- */
  .section-spacer {
    height: 35vh;
    position: relative;
  }

  /* ---- Cinematic Sections ---- */
  .cine-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--space-8) 0;
    box-sizing: border-box;
    overflow: hidden;
  }

  /* Per-section dark background with gradient edges */
  .cine-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(10, 0, 0, 0.55) 12%,
      rgba(10, 0, 0, 0.55) 88%,
      transparent 100%
    );
    pointer-events: none;
    z-index: 0;
  }

  /* Hero: no top fade (starts dark) */
  .hero::before {
    background: linear-gradient(
      to bottom,
      rgba(10, 0, 0, 0.55) 0%,
      rgba(10, 0, 0, 0.55) 88%,
      transparent 100%
    );
  }

  /* Content sits above the background */
  .cine-section > :global(*) {
    position: relative;
    z-index: 1;
  }

  .cine-heading {
    text-align: center;
    will-change: transform, opacity;
  }

  .cine-content {
    opacity: 0;
    will-change: transform, opacity;
  }

  /* ---- Reduced Motion ---- */
  @media (prefers-reduced-motion: reduce) {
    .cine-content {
      opacity: 1 !important;
      transform: none !important;
    }

    .cine-heading {
      transform: none !important;
      opacity: 1 !important;
    }
  }

  /* ---- Hero ---- */
  .hero {
    text-align: center;
    padding-bottom: calc(var(--space-8) + 124px);
  }

  .hero .cine-content {
    opacity: 1;
  }

  .hero-label {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wider);
    margin-bottom: var(--space-6);
  }

  .hero :global(.hero-title) {
    font-size: var(--text-5xl);
    color: var(--text-primary);
    text-shadow:
      3px 3px 0 rgba(212, 84, 94, 0.3),
      -1px -1px 0 rgba(0, 0, 0, 0.5);
    margin-bottom: var(--space-6);
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-6);
    max-width: 650px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-quote-block {
    position: relative;
    max-width: 700px;
    margin: 0 auto var(--space-8);
    text-align: left;
  }

  .hero-sticker {
    position: absolute;
    width: 280px;
    height: auto;
    opacity: 0.7;
    transform: rotate(-5deg);
    filter: drop-shadow(0 4px 16px rgba(212, 84, 94, 0.25));
    border-radius: var(--radius-md);
    top: 50%;
    left: -75px;
    translate: 0 -50%;
    z-index: 0;
    pointer-events: none;
  }

  .hero-quote {
    position: relative;
    z-index: 1;
    border-left: 3px solid var(--text-accent);
    padding-left: var(--space-5);
    margin: 0;
    margin-left: 180px;
  }

  .hero-quote p {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    font-style: italic;
    margin-bottom: var(--space-2);
  }

  .hero-quote cite {
    font-size: var(--text-xs);
    color: var(--text-accent);
    font-style: normal;
    opacity: 0.7;
  }

  @media (max-width: 768px) {
    .hero-sticker {
      position: relative;
      display: block;
      margin: 0 auto var(--space-4);
      width: 200px;
      top: auto;
      left: auto;
      translate: none;
      transform: rotate(-3deg);
      opacity: 0.8;
    }

    .hero-quote {
      margin-left: 0;
      border-left: none;
      border-top: 3px solid var(--text-accent);
      padding-left: 0;
      padding-top: var(--space-4);
      text-align: center;
    }
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
    justify-content: center;
  }

  /* ---- Buttons ---- */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    text-decoration: none;
    border-radius: var(--radius-sm);
    cursor: pointer;
    transition: all var(--transition-normal);
  }

  .btn:focus-visible {
    outline: 2px solid var(--text-accent);
    outline-offset: 2px;
  }

  .btn-primary {
    background: var(--text-accent);
    color: var(--bg-base);
    border: 2px solid var(--text-accent);
  }

  .btn-primary:hover {
    background: var(--color-white);
    color: var(--bg-base);
    border-color: var(--color-white);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text-primary);
    border: 2px solid var(--border-muted);
  }

  .btn-secondary:hover {
    border-color: var(--border-primary);
    color: var(--text-accent);
  }

  /* ---- Section Headings ---- */
  .section-heading {
    font-size: var(--text-4xl);
    text-align: center;
    margin-bottom: var(--space-4);
    color: var(--text-primary);
  }

  .section-intro {
    text-align: center;
    max-width: 650px;
    margin: 0 auto var(--space-10);
    font-size: var(--text-lg);
    color: var(--text-secondary);
  }

  .section-cta {
    text-align: center;
    margin-top: var(--space-8);
  }

  /* ---- Section Quote (blockquote style for section intros) ---- */
  .section-quote {
    text-align: center;
    max-width: 700px;
    margin: 0 auto var(--space-10);
    padding: var(--space-5) var(--space-6);
    border-left: 3px solid var(--text-accent);
    border-right: 3px solid var(--text-accent);
    background: rgba(212, 84, 94, 0.04);
    border-radius: var(--radius-sm);
  }

  .section-quote p {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    font-style: italic;
    margin-bottom: var(--space-2);
  }

  .section-quote cite {
    font-size: var(--text-xs);
    color: var(--text-accent);
    font-style: normal;
    opacity: 0.7;
  }

  /* ---- Card Quote ---- */
  :global(.card-quote) {
    font-style: italic;
    color: var(--text-muted) !important;
    font-size: var(--text-xs) !important;
    border-left: 2px solid var(--border-primary);
    padding-left: var(--space-3);
    margin-top: var(--space-3) !important;
  }

  /* ---- Pillars ---- */
  .pillars-layout {
    position: relative;
  }

  .pillars-bg-sticker {
    position: absolute;
    top: 100%;
    left: 50%;
    translate: -50% -60%;
    width: 700px;
    height: auto;
    opacity: 0.15;
    filter: drop-shadow(0 0 30px rgba(212, 84, 94, 0.15));
    pointer-events: none;
    z-index: 0;
  }

  .pillars-grid {
    position: relative;
    z-index: 1;
    gap: var(--space-6);
  }

  .pillar-card {
    background: rgba(26, 29, 35, 0.5);
    backdrop-filter: blur(4px);
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-md);
    padding: var(--space-8);
    position: relative;
  }

  .pillar-number {
    font-family: var(--font-display);
    font-size: var(--text-5xl);
    font-weight: 700;
    color: var(--text-accent);
    opacity: 0.15;
    position: absolute;
    top: var(--space-4);
    right: var(--space-6);
    line-height: 1;
  }

  .pillar-title {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    color: var(--text-accent);
    text-transform: uppercase;
    margin-bottom: var(--space-4);
  }

  .pillar-card p {
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .pillar-quote {
    margin-top: var(--space-4);
    padding-left: var(--space-4);
    border-left: 2px solid var(--border-primary);
    font-style: italic;
    color: var(--text-muted) !important;
    font-size: var(--text-sm);
  }

  .quote-attr {
    display: block;
    margin-top: var(--space-1);
    font-style: normal;
    color: var(--text-accent);
    font-size: var(--text-xs);
    opacity: 0.7;
  }

  /* ---- Layers ---- */
  .layers-layout {
    position: relative;
  }

  .layers-bg-sticker {
    position: absolute;
    top: 50%;
    left: 50%;
    translate: -50% -50%;
    width: 950px;
    height: auto;
    opacity: 0.1;
    filter: drop-shadow(0 0 30px rgba(212, 84, 94, 0.15));
    pointer-events: none;
    z-index: 0;
  }

  :global(.layer-card) {
    position: relative;
    overflow: hidden;
  }

  :global(.layer-card-bg) {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 55%;
    height: auto;
    opacity: 0.08;
    pointer-events: none;
    z-index: 0;
    object-fit: contain;
    object-position: bottom right;
  }

  /* Per-card position overrides — tweak these freely */
  :global(.layer-bg-linux) {
    bottom: -24px;
    right: -42px;
    width: 62%;
  }

  :global(.layer-bg-google) {
    bottom: -10%;
    right: -5%;
    width: 50%;
  }

  :global(.layer-bg-brain) {
    bottom: -20px;
    right: -15px;
    width: 50%;
  }

  :global(.layer-card .card-inner) {
    position: relative;
    z-index: 1;
  }

  .layers-grid {
    position: relative;
    z-index: 1;
  }

  /* ---- Card Source ---- */
  :global(.card-source) {
    font-size: var(--text-xs) !important;
    color: var(--text-muted) !important;
    font-style: italic;
    margin-top: var(--space-3) !important;
    opacity: 0.6;
  }

  /* ---- Card Meta ---- */
  :global(.card-meta) {
    font-family: var(--font-display);
    font-size: var(--text-xs) !important;
    color: var(--text-muted) !important;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    margin-top: var(--space-3) !important;
    padding-top: var(--space-3);
    border-top: 1px solid var(--border-muted);
  }

  /* ---- Guides Layout ---- */
  .guides-layout {
    position: relative;
  }

  .guides-bg-sticker {
    position: absolute;
    top: 50%;
    left: 50%;
    translate: 20% -40%;
    width: 800px;
    height: auto;
    opacity: 0.12;
    filter: drop-shadow(0 0 30px rgba(212, 84, 94, 0.15));
    pointer-events: none;
    z-index: 0;
  }

  .guides-grid {
    position: relative;
    z-index: 1;
  }

  /* ---- Guide Preview Cards ---- */
  .guide-preview-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    padding: var(--space-6);
    background: var(--bg-card);
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: all var(--transition-normal);
    position: relative;
    overflow: hidden;
  }

  .guide-card-bg {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 45%;
    height: auto;
    opacity: 0.08;
    pointer-events: none;
    z-index: 0;
    object-fit: contain;
    object-position: bottom right;
  }

  /* Per-guide position overrides — tweak these freely */
  .guide-bg-firefox {
    bottom: -10%;
    right: -5%;
    width: 50%;
  }

  .guide-bg-arch {
    bottom: -5%;
    right: -5%;
    width: 45%;
  }

  .guide-bg-opensource {
    bottom: -10%;
    right: -5%;
    width: 45%;
  }

  .guide-preview-card:hover {
    border-color: var(--text-accent);
    transform: translateY(-2px);
  }

  .guide-preview-card h3 {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    color: var(--text-primary);
    text-transform: uppercase;
  }

  .guide-preview-card p {
    color: var(--text-secondary);
    font-size: var(--text-sm);
    line-height: var(--leading-relaxed);
  }

  /* ---- Honesty Layout ---- */
  .honesty-layout {
    position: relative;
  }

  .honesty-bg-sticker {
    position: absolute;
    top: 50%;
    left: 50%;
    translate: -100% -50%;
    width: 1000px;
    height: auto;
    opacity: 0.12;
    filter: drop-shadow(0 0 30px rgba(212, 84, 94, 0.15));
    pointer-events: none;
    z-index: 0;
  }


  /* ---- Disclaimer ---- */
  .disclaimer-heading {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    color: var(--text-accent);
    text-transform: uppercase;
    text-align: center;
    margin-bottom: var(--space-4);
  }

  .disclaimer-box {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-8);
    background: rgba(26, 29, 35, 0.5);
    backdrop-filter: blur(4px);
    border: 1px dashed var(--border-muted);
    border-radius: var(--radius-md);
    position: relative;
    z-index: 1;
  }

  .disclaimer-box::before {
    content: '';
    position: absolute;
    top: -6px;
    left: var(--space-6);
    width: 60px;
    height: 20px;
    background: repeating-linear-gradient(
      -45deg,
      rgba(212, 84, 94, 0.1),
      rgba(212, 84, 94, 0.1) 4px,
      transparent 4px,
      transparent 8px
    );
    transform: rotate(-2deg);
  }

  .disclaimer-box p {
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    font-size: var(--text-sm);
  }

  /* ---- Extras Grid ---- */
  .extras-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
    max-width: 900px;
    margin: 0 auto;
  }

  @media (max-width: 767px) {
    .extras-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (min-width: 768px) and (max-width: 1023px) {
    .extras-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .extra-card {
    padding: var(--space-5);
    background: var(--bg-card);
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: all var(--transition-normal);
    position: relative;
    overflow: hidden;
  }

  .extra-card::after {
    content: attr(data-emoji);
    position: absolute;
    top: -10px;
    right: -5px;
    font-size: 4rem;
    opacity: 0.1;
    pointer-events: none;
    z-index: 0;
  }

  .extra-card:hover {
    border-color: var(--text-accent);
    transform: translateY(-2px);
  }

  .extra-card h3 {
    font-family: var(--font-display);
    font-size: var(--text-base);
    color: var(--text-primary);
    text-transform: uppercase;
    margin-bottom: var(--space-2);
  }

  .extra-card p {
    color: var(--text-secondary);
    font-size: var(--text-xs);
    line-height: var(--leading-relaxed);
  }

  .extras-brofist-cell {
    display: flex;
    align-items: flex-end;
    justify-content: center;
    align-self: stretch;
  }

  .extras-brofist {
    width: 80px;
    height: auto;
    opacity: 0.5;
    filter: drop-shadow(0 0 20px rgba(212, 84, 94, 0.2));
    pointer-events: none;
    max-height: 100%;
    object-fit: contain;
    /* Tweak these freely: scale, move (X, Y), rotate */
    transform: scale(2.25, 1.35) translate(0px, 0px) rotate(0deg);
  }

  @media (max-width: 1023px) {
    .extras-brofist-cell {
      display: none;
    }
  }
</style>
